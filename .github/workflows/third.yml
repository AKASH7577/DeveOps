name: Third Pipeline

on:
  workflow_dispatch:
  # push:
  #   branches:
  #     - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Print Hello
        run: echo "Hello, GitHub Actions!"